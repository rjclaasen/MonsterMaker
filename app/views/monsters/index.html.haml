%p#notice= notice
%h1 Monsters
%table
  %thead
    %tr
      %th Name
      %th Challenge
      %th Author
      %th{:colspan => "3"}
  %tbody
    - @monsters.each do |monster|
      %tr
        %td= monster.name
        %td= monster.challenge
        %td= monster.user&.username
        %td= link_to 'Show', monster
        - if user_signed_in? && monster.author == current_user
          %td= link_to 'Edit', edit_monster_path(monster)
          %td= link_to 'Delete', monster, method: :delete, data: { confirm: I18n.t("monsters.confirm_delete", monster_name: monster.name) } 
%br/
= link_to 'New Monster', new_monster_path
